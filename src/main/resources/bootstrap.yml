#Server Configuration
server:
  port: 10002

#Spring Configuration
spring:
  application:
    name: batch-service
  cloud:
    config:
      uri: https://gambit-config-server-brash-porcupine.cfapps.io:8888

#Eureka Configuration
eureka:
  client:
    register-with-eureka: true

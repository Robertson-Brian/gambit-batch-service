#Server Configuration
server:
  port: 10002

#Spring Configuration
spring:
  application:
    name: batch-service
  cloud:
    config:
      uri: ${CONFIG_HOME}

#Eureka Configuration
eureka:
  client:
    register-with-eureka: true
